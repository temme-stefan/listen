# .env – wird NICHT in Git eingecheckt, liegt manuell auf dem Server
# Erzeugen:
#   php -r "echo password_hash('meinPasswort', PASSWORD_BCRYPT);"
#   openssl rand -hex 32   (für JWT_SECRET)

PASSWORD_HASH=$2y$10$BEISPIEL_BITTE_ERSETZEN
JWT_SECRET=bitte-einen-langen-zufaelligen-string-eintragen

